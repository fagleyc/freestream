# _attic — parked dead code (not imported by anything)

- `runs_adapters/` — stale, older copies of `freestream/adapters/*.py`
  that had been sitting inside the `runs/` OUTPUT folder (timestamps
  predate the live adapters). Moved here 2026-07-08 instead of deleting
  outright; the canonical adapters live in `freestream/adapters/`.
