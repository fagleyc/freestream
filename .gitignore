# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Experiment run data (HDF5/mat/xlsx outputs) — not source
runs/

# Editor/OS noise
.vscode/
.idea/
Thumbs.db
.DS_Store

# Local-only markers
_MOVED_README.txt

# Vendor manuals too large for the repo (kept local only)
devices/ac_delta/info/delta-c2000-manual.pdf

# Runtime/test state (regenerated every session)
devices/lswt_sting/sting_state.json
devices/lswt_sting/sting_state.tmp
devices/tests/_state_scratch.json

# cloud-sync artifacts
*conflicted copy*
.git_broken_cloudsync/
# lswt_traverse reference assets kept on disk, not in the repo:
# 57 MB of rig photos and the old TunnelVision build outputs
devices/lswt_traverse/LSWT-S Traverse Photos/
devices/lswt_traverse/Tool_SSWT_Traverse/bin/
devices/lswt_traverse/Tool_SSWT_Traverse/obj/
